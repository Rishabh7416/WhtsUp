import {View, Text} from 'react-native';
import React from 'react';

export default function StylingComp() {
  return (
    <View
      style={{
        marginVertical: 10,
        // textDecorationLine: 'underline',
        backgroundColor: 'grey',
        height: 0.5,
      }}
    />
  );
}
